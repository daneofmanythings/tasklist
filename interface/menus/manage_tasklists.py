from interface.menu import Menu


class ManageTasklists(Menu):
    HEADER = (
        'MAIN / MANAGE_TASKLISTS /',
    )
    MENU = (
        "1) Go Back",
    )
    OPTIONS = {
        "1": 0,
    }
